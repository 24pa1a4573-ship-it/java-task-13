# java-task-13
Java program demonstrating JDBC connectivity with MySQL to perform CRUD operations on a student database using PreparedStatement. It prevents SQL injection, handles exceptions, and properly manages database resources.
